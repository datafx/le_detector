; LE Gear Detector - ESP32-WROOM-32U DevKitC
;
; Derived in part from nyanBOX (https://github.com/jbohack/nyanBOX)
; Copyright (c) 2025 jbohack - MIT License

[env:esp32dev]
platform  = espressif32
board     = esp32dev
framework = arduino

monitor_speed = 115200
upload_speed  = 921600

; Default partition table reserves space for OTA (dual app slots); this
; firmware has no OTA update path, so use a single ~3MB app partition instead.
board_build.partitions = huge_app.csv

lib_deps =
    olikraus/U8g2 @ ^2.35.30

build_flags =
    -DCORE_DEBUG_LEVEL=0
